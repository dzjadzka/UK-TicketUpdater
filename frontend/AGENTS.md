# Frontend Agent Instructions

- Manage dependencies inside `frontend` with npm (`npm install`, `npm run dev`, etc.).
- Prefer using the root-level scripts (`npm run dev:frontend`, `npm run build:frontend`, `npm run lint:frontend`) when working from the repo root.
- This file overrides root-level agent instructions for files within the `frontend/` directory.
