# Agent Instructions

- Use the provided root npm scripts when working with the frontend (`dev:frontend`, `build:frontend`, `lint:frontend`).
- Keep README instructions up to date when adding setup or workflow changes.
- If you add another `AGENTS.md` inside a subdirectory, that file takes precedence for files under that subpath.
